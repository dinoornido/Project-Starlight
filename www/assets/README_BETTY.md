# Betty assets

- `betty.svg` — primary face for StarGate (also embedded as data-URI in stargate.html so she never shows broken image).
- Drop `betty_avatar.jpg` (256x256) on the live nginx `/assets/` when you have server access so the photo version can load first.

R2 · build tab is the direct implementation channel with R2. Betty stays on ops.
